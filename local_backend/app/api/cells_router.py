"""Router for cell-related endpoints.

Placeholder for the scaffold phase. Real endpoints (open one cell,
open multiple cells, list cells, etc.) will be added once the
hardware client and cell service are implemented.
"""

from fastapi import APIRouter

router = APIRouter(prefix="/cells", tags=["cells"])
