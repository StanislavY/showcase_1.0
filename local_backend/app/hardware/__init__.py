"""Hardware abstraction layer.

Encapsulates all interaction with the physical postamat hardware
(Arduino cell controller, barcode scanner). The rest of the
application must talk to the hardware only through this layer.
"""
