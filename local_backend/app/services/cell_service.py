"""Cell service: business logic for cell operations.

Placeholder in the scaffold phase. Will expose methods like
``open_cell(cell_number)`` and ``open_cells(cell_numbers)`` and
delegate the actual serial I/O to ``app.hardware.hardware_client``.
"""
